<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GDM2026.ProductsPage"
             BackgroundColor="#0B0B0B">
    <ContentPage.Resources>
        <Color x:Key="GoldText">#D4AF37</Color>
        <Color x:Key="CardBackground">#151515</Color>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="18">
            <Border
                Background="{StaticResource CardBackground}"
                Stroke="#000000"
                StrokeThickness="1.5"
                Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="18" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="16">
                    <Label
                        Text="Catalogue produits"
                        FontSize="28"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}" />

                    <Label
                        Text="Gérez la création, la navigation et la mise à jour des produits avec le même confort que l'Actualité."
                        FontSize="16"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}"
                        Opacity="0.85" />

                    <VerticalStackLayout Spacing="10">
                        <Button
                            Text="+ Nouveau produit"
                            BackgroundColor="#303030"
                            TextColor="{StaticResource GoldText}"
                            Command="{Binding ToggleFormCommand}" />
                        <Button
                            Text="Mettre à jour un produit"
                            BackgroundColor="#303030"
                            TextColor="{StaticResource GoldText}"
                            Clicked="OnEditProductClicked" />
                    </VerticalStackLayout>

                    <Border
                        IsVisible="{Binding IsFormVisible}"
                        Background="#1E1E1E"
                        Padding="14"
                        Stroke="#2D2D2D">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="14" />
                        </Border.StrokeShape>
                        <VerticalStackLayout Spacing="12">
                            <Label
                                Text="Nouveau produit"
                                FontAttributes="Bold"
                                TextColor="{StaticResource GoldText}"
                                FontSize="20" />

                            <Label
                                Text="Renseignez les informations du produit puis validez pour l'ajouter au catalogue."
                                TextColor="{StaticResource GoldText}"
                                FontSize="13"
                                Opacity="0.8" />

                            <VerticalStackLayout Spacing="12">
                                <VerticalStackLayout Spacing="6">
                                    <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                        <Label Text="Nom du produit" TextColor="{StaticResource GoldText}" FontAttributes="Bold" />
                                        <Label Text="⚠" TextColor="Tomato" FontAttributes="Bold" IsVisible="{Binding IsNameMissing}" />
                                    </HorizontalStackLayout>
                                    <Entry Placeholder="Entrez le nom"
                                           Text="{Binding NewProductName}"
                                           TextColor="{StaticResource GoldText}"
                                           PlaceholderColor="#777"
                                           BackgroundColor="#121212" />
                                </VerticalStackLayout>

                                <VerticalStackLayout Spacing="6">
                                    <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                        <Label Text="Description courte" TextColor="{StaticResource GoldText}" FontAttributes="Bold" />
                                        <Label Text="⚠" TextColor="Tomato" FontAttributes="Bold" IsVisible="{Binding IsShortDescriptionMissing}" />
                                    </HorizontalStackLayout>
                                    <Editor Placeholder="Résumé du produit"
                                            HeightRequest="80"
                                            Text="{Binding NewProductShortDescription}"
                                            TextColor="{StaticResource GoldText}"
                                            PlaceholderColor="#777"
                                            BackgroundColor="#121212" />
                                </VerticalStackLayout>

                                <VerticalStackLayout Spacing="6">
                                    <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                        <Label Text="Description complète" TextColor="{StaticResource GoldText}" FontAttributes="Bold" />
                                        <Label Text="⚠" TextColor="Tomato" FontAttributes="Bold" IsVisible="{Binding IsFullDescriptionMissing}" />
                                    </HorizontalStackLayout>
                                    <Editor Placeholder="Description détaillée"
                                            HeightRequest="140"
                                            Text="{Binding NewProductFullDescription}"
                                            TextColor="{StaticResource GoldText}"
                                            PlaceholderColor="#777"
                                            BackgroundColor="#121212" />
                                </VerticalStackLayout>

                                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                                    <VerticalStackLayout Grid.Column="0" Spacing="6">
                                        <HorizontalStackLayout Spacing="6">
                                            <Label Text="Prix" TextColor="{StaticResource GoldText}" FontAttributes="Bold" />
                                            <Label Text="⚠" TextColor="Tomato" FontAttributes="Bold" IsVisible="{Binding IsPriceMissing}" />
                                        </HorizontalStackLayout>
                                        <Entry Placeholder="ex : 14.90"
                                               Keyboard="Numeric"
                                               Text="{Binding ProductPriceText}"
                                               TextColor="{StaticResource GoldText}"
                                               PlaceholderColor="#777"
                                               BackgroundColor="#121212" />
                                    </VerticalStackLayout>
                                    <VerticalStackLayout Grid.Column="1" Spacing="6">
                                        <HorizontalStackLayout Spacing="6">
                                            <Label Text="Quantité" TextColor="{StaticResource GoldText}" FontAttributes="Bold" />
                                            <Label Text="⚠" TextColor="Tomato" FontAttributes="Bold" IsVisible="{Binding IsQuantityMissing}" />
                                        </HorizontalStackLayout>
                                        <Entry Placeholder="ex : 25"
                                               Keyboard="Numeric"
                                               Text="{Binding ProductQuantityText}"
                                               TextColor="{StaticResource GoldText}"
                                               PlaceholderColor="#777"
                                               BackgroundColor="#121212" />
                                    </VerticalStackLayout>
                                </Grid>

                                <VerticalStackLayout Spacing="6">
                                    <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                        <Label Text="Catégorie" TextColor="{StaticResource GoldText}" FontAttributes="Bold" />
                                        <Label Text="⚠" TextColor="Tomato" FontAttributes="Bold" IsVisible="{Binding IsCategoryMissing}" />
                                    </HorizontalStackLayout>
                                    <Label Text="{Binding CategoryStatusMessage}" FontSize="12" TextColor="{StaticResource GoldText}" Opacity="0.85" />
                                    <ActivityIndicator IsRunning="{Binding IsCategoryLoading}" IsVisible="{Binding IsCategoryLoading}" Color="{StaticResource GoldText}" />
                                    <Picker ItemsSource="{Binding AvailableCategories}"
                                            SelectedItem="{Binding SelectedCategory}"
                                            ItemDisplayBinding="{Binding DisplayName}"
                                            Title="Choisissez une catégorie"
                                            IsEnabled="{Binding IsCategorySelectionEnabled}"
                                            TextColor="{StaticResource GoldText}"
                                            BackgroundColor="#101010" />
                                </VerticalStackLayout>

                                <VerticalStackLayout Spacing="8">
                                    <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                        <Label Text="{Binding SelectedImageName}" TextColor="{StaticResource GoldText}" FontAttributes="Bold" />
                                        <Label Text="⚠" TextColor="Tomato" FontAttributes="Bold" IsVisible="{Binding IsImageMissing}" />
                                    </HorizontalStackLayout>

                                    <Border BackgroundColor="#1B1B1B" Stroke="#2D2D2D" Padding="12">
                                        <VerticalStackLayout Spacing="8">
                                            <SearchBar Placeholder="Rechercher une image"
                                                       Text="{Binding ImageSearchTerm}"
                                                       TextColor="{StaticResource GoldText}"
                                                       PlaceholderColor="#7E7030"
                                                       CancelButtonColor="{StaticResource GoldText}"
                                                       BackgroundColor="#101010" />

                                            <Label Text="{Binding ImageLibraryMessage}" FontSize="12" TextColor="{StaticResource GoldText}" Opacity="0.8" />

                                            <ActivityIndicator IsRunning="{Binding IsImageLibraryLoading}" IsVisible="{Binding IsImageLibraryLoading}" Color="{StaticResource GoldText}" />

                                            <CollectionView ItemsSource="{Binding FilteredImageLibrary}"
                                                            SelectionMode="Single"
                                                            SelectedItem="{Binding SelectedLibraryImage}"
                                                            HeightRequest="200">
                                                <CollectionView.ItemsLayout>
                                                    <GridItemsLayout Orientation="Vertical" Span="3" />
                                                </CollectionView.ItemsLayout>
                                                <CollectionView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Padding="4" Margin="4" BackgroundColor="#111" Stroke="#333">
                                                            <Border.StrokeShape>
                                                                <RoundRectangle CornerRadius="10" />
                                                            </Border.StrokeShape>
                                                            <VerticalStackLayout Spacing="4">
                                                                <Image Source="{Binding FullUrl}" HeightRequest="70" Aspect="AspectFill" />
                                                                <Label Text="{Binding DisplayName}" FontSize="11" TextColor="{StaticResource GoldText}" LineBreakMode="TailTruncation" HorizontalTextAlignment="Center" />
                                                            </VerticalStackLayout>
                                                        </Border>
                                                    </DataTemplate>
                                                </CollectionView.ItemTemplate>
                                            </CollectionView>
                                        </VerticalStackLayout>
                                    </Border>
                                </VerticalStackLayout>

                                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                                    <Button Text="Réinitialiser"
                                            Command="{Binding ResetFormCommand}"
                                            BackgroundColor="#353535"
                                            TextColor="{StaticResource GoldText}" />
                                    <Button Grid.Column="1"
                                            Text="Créer le produit"
                                            Command="{Binding SubmitProductCommand}"
                                            IsEnabled="{Binding IsSubmitEnabled}"
                                            BackgroundColor="#C18F1A"
                                            TextColor="{StaticResource GoldText}" />
                                </Grid>

                                <Label Text="{Binding CreationStatusMessage}"
                                       TextColor="{Binding CreationStatusColor}"
                                       FontSize="13"
                                       HorizontalTextAlignment="Center"
                                       LineBreakMode="WordWrap" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </Border>

                    <Border
                        Background="#1E1E1E"
                        Padding="14"
                        Stroke="#2D2D2D">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="14" />
                        </Border.StrokeShape>
                        <VerticalStackLayout Spacing="12">
                            <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                                <Label Text="✏️" FontSize="22" VerticalOptions="Center" />
                                <Label Text="Modifier un produit" FontAttributes="Bold" TextColor="{StaticResource GoldText}" FontSize="20" VerticalOptions="Center" />
                            </HorizontalStackLayout>
                            <Label Text="{Binding CatalogFilterStatus}"
                                   TextColor="{StaticResource GoldText}" FontSize="13" Opacity="0.8" />

                            <!-- Sélection rapide d'un produit (ordre alphabétique) -->
                            <Border BackgroundColor="#121212" Stroke="#2D2D2D" Padding="12">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10" />
                                </Border.StrokeShape>
                                <VerticalStackLayout Spacing="8">
                                    <HorizontalStackLayout Spacing="6">
                                        <Label Text="📦" FontSize="16" VerticalOptions="Center" />
                                        <Label Text="Accès rapide produit (A-Z)" TextColor="{StaticResource GoldText}" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                                    </HorizontalStackLayout>
                                    <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="{StaticResource GoldText}" HeightRequest="20" />
                                    <Picker
                                        x:Name="ProductPicker"
                                        Title="Choisir un produit à modifier..."
                                        ItemsSource="{Binding SortedProducts}"
                                        ItemDisplayBinding="{Binding DisplayName}"
                                        SelectedIndexChanged="OnProductPickerSelectedIndexChanged"
                                        TextColor="{StaticResource GoldText}"
                                        TitleColor="#7E7030"
                                        BackgroundColor="#0D0D0D"
                                        HeightRequest="50" />
                                </VerticalStackLayout>
                            </Border>

                            <Button
                                Text="↻ Actualiser la liste"
                                Command="{Binding RefreshCommand}"
                                BackgroundColor="#303030"
                                TextColor="{StaticResource GoldText}"
                                FontSize="13"
                                HeightRequest="40" />
                        </VerticalStackLayout>
                    </Border>
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
