<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             xmlns:viewmodels="clr-namespace:GDM2026.ViewModels"
             x:Class="GDM2026.Views.AjouterPointsFidelitePage"
             Title="Ajouter Points Fidélité"
             BackgroundColor="#0B0B0B">

    <ContentPage.BindingContext>
        <viewmodels:AjouterPointsFideliteViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <Color x:Key="GoldText">#D4AF37</Color>
        <Color x:Key="CardBackground">#151515</Color>
        <Color x:Key="RushColor">#FF6B35</Color>
        <Color x:Key="SuccessColor">#4CAF50</Color>
        <Color x:Key="ErrorColor">#F44336</Color>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*">

        <!-- Section Scanner (occupe tout l'espace) -->
        <Grid Grid.Row="0" Grid.RowSpan="2" IsVisible="{Binding IsScanning}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header Scanner avec icône Rush -->
            <Grid Grid.Row="0" Padding="24,24,24,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="50" />
                </Grid.ColumnDefinitions>

                <!-- Icône Rush à gauche -->
                <Border
                    Grid.Column="0"
                    WidthRequest="44"
                    HeightRequest="44"
                    StrokeShape="RoundRectangle 22"
                    Stroke="{Binding IsRushModeActive, Converter={x:Null}}"
                    BackgroundColor="{Binding IsRushModeActive, Converter={x:Null}}"
                    HorizontalOptions="Start"
                    VerticalOptions="Start">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding OpenRushConfigCommand}" />
                    </Border.GestureRecognizers>
                    <Border.Triggers>
                        <DataTrigger TargetType="Border" Binding="{Binding IsRushModeActive}" Value="True">
                            <Setter Property="BackgroundColor" Value="{StaticResource RushColor}" />
                            <Setter Property="Stroke" Value="{StaticResource RushColor}" />
                        </DataTrigger>
                        <DataTrigger TargetType="Border" Binding="{Binding IsRushModeActive}" Value="False">
                            <Setter Property="BackgroundColor" Value="Transparent" />
                            <Setter Property="Stroke" Value="{StaticResource GoldText}" />
                        </DataTrigger>
                    </Border.Triggers>
                    <Label
                        Text="⚡"
                        FontSize="22"
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        TextColor="White" />
                </Border>

                <!-- Titre et status au centre -->
                <VerticalStackLayout Grid.Column="1" Spacing="8">
                    <Label
                        Text="Ajouter Points Fidélité"
                        FontSize="22"
                        FontAttributes="Bold"
                        TextColor="{StaticResource GoldText}"
                        HorizontalTextAlignment="Center" />
                    <Label
                        Text="{Binding StatusMessage}"
                        FontSize="14"
                        TextColor="{StaticResource GoldText}"
                        Opacity="0.8"
                        HorizontalTextAlignment="Center" />
                </VerticalStackLayout>

                <!-- Bouton désactiver Rush à droite (visible si mode Rush actif) -->
                <Border
                    Grid.Column="2"
                    IsVisible="{Binding IsRushModeActive}"
                    WidthRequest="44"
                    HeightRequest="44"
                    StrokeShape="RoundRectangle 22"
                    BackgroundColor="#333"
                    Stroke="#666"
                    HorizontalOptions="End"
                    VerticalOptions="Start">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding DeactivateRushModeCommand}" />
                    </Border.GestureRecognizers>
                    <Label
                        Text="✕"
                        FontSize="18"
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        TextColor="#AAA" />
                </Border>
            </Grid>

            <!-- Scanner Camera -->
            <zxing:CameraBarcodeReaderView
                x:Name="BarcodeReader"
                Grid.Row="1"
                Margin="24"
                BarcodesDetected="OnBarcodesDetected" />

            <!-- Status Loading -->
            <VerticalStackLayout Grid.Row="2" Padding="24,12" Spacing="8">
                <ActivityIndicator
                    IsRunning="{Binding IsLoading}"
                    IsVisible="{Binding IsLoading}"
                    Color="{StaticResource GoldText}" />
            </VerticalStackLayout>

            <!-- Boutons Scanner -->
            <HorizontalStackLayout Grid.Row="3"
                                   Padding="24,12,24,24"
                                   Spacing="12"
                                   HorizontalOptions="Center">
                <Button
                    Text="Annuler"
                    Command="{Binding CancelCommand}"
                    BackgroundColor="Transparent"
                    BorderColor="{StaticResource GoldText}"
                    BorderWidth="1.5"
                    TextColor="{StaticResource GoldText}"
                    CornerRadius="10"
                    WidthRequest="120"
                    HeightRequest="50" />

                <Button
                    x:Name="TorchButton"
                    Text="Torche"
                    Clicked="OnTorchClicked"
                    BackgroundColor="#1A1A1A"
                    TextColor="{StaticResource GoldText}"
                    CornerRadius="10"
                    WidthRequest="120"
                    HeightRequest="50" />
            </HorizontalStackLayout>
        </Grid>

        <!-- Section Formulaires (quand le scanner est caché) -->
        <ScrollView Grid.Row="0" Grid.RowSpan="2" IsVisible="{Binding ShowClientInfo}">
            <VerticalStackLayout Spacing="24" Padding="24">

                <!-- Header -->
                <VerticalStackLayout Spacing="8">
                    <Label
                        Text="Ajouter Points Fidélité"
                        FontSize="28"
                        FontAttributes="Bold"
                        TextColor="{StaticResource GoldText}"
                        HorizontalTextAlignment="Center" />
                    <Label
                        Text="{Binding StatusMessage}"
                        FontSize="14"
                        TextColor="{StaticResource GoldText}"
                        Opacity="0.8"
                        HorizontalTextAlignment="Center" />
                </VerticalStackLayout>

                <!-- Informations Client -->
                <Border
                    IsVisible="{Binding ShowClientInfo}"
                    Stroke="#000000"
                    StrokeThickness="1.5"
                    BackgroundColor="{StaticResource CardBackground}"
                    Padding="20"
                    StrokeShape="RoundRectangle 14">
                    <VerticalStackLayout Spacing="12">
                        <Label
                            Text="Informations Client"
                            FontSize="18"
                            FontAttributes="Bold"
                            TextColor="{StaticResource GoldText}" />

                        <BoxView
                            HeightRequest="1"
                            Color="{StaticResource GoldText}"
                            Opacity="0.3" />

                        <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto,Auto" RowSpacing="8">
                            <Label
                                Grid.Row="0" Grid.Column="0"
                                Text="Nom :"
                                FontSize="14"
                                FontAttributes="Bold"
                                TextColor="{StaticResource GoldText}"
                                VerticalOptions="Center"
                                Margin="0,0,12,0" />
                            <Label
                                Grid.Row="0" Grid.Column="1"
                                Text="{Binding ClientName}"
                                FontSize="14"
                                TextColor="{StaticResource GoldText}"
                                VerticalOptions="Center" />

                            <Label
                                Grid.Row="1" Grid.Column="0"
                                Text="Points actuels :"
                                FontSize="14"
                                FontAttributes="Bold"
                                TextColor="{StaticResource GoldText}"
                                VerticalOptions="Center"
                                Margin="0,0,12,0" />
                            <Label
                                Grid.Row="1" Grid.Column="1"
                                Text="{Binding ClientPoints}"
                                FontSize="14"
                                TextColor="{StaticResource GoldText}"
                                VerticalOptions="Center" />

                            <Label
                                Grid.Row="2" Grid.Column="0"
                                Text="Valeur :"
                                FontSize="14"
                                FontAttributes="Bold"
                                TextColor="{StaticResource GoldText}"
                                VerticalOptions="Center"
                                Margin="0,0,12,0" />
                            <Label
                                Grid.Row="2" Grid.Column="1"
                                Text="{Binding ClientPointsInEuros}"
                                FontSize="14"
                                TextColor="{StaticResource GoldText}"
                                VerticalOptions="Center" />
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <!-- Formulaire Saisie Montant -->
                <Border
                    IsVisible="{Binding ShowAmountForm}"
                    Stroke="#000000"
                    StrokeThickness="1.5"
                    BackgroundColor="{StaticResource CardBackground}"
                    Padding="20"
                    StrokeShape="RoundRectangle 14">
                    <VerticalStackLayout Spacing="16">
                        <Label
                            Text="Montant de l'Achat"
                            FontSize="18"
                            FontAttributes="Bold"
                            TextColor="{StaticResource GoldText}" />

                        <BoxView
                            HeightRequest="1"
                            Color="{StaticResource GoldText}"
                            Opacity="0.3" />

                        <VerticalStackLayout Spacing="8">
                            <Label
                                Text="Saisissez le montant de l'achat (€) :"
                                FontSize="14"
                                TextColor="{StaticResource GoldText}"
                                Opacity="0.9" />

                            <Label
                                Text="1€ = 1 couronne (point de fidélité)"
                                FontSize="12"
                                TextColor="{StaticResource GoldText}"
                                Opacity="0.7"
                                FontAttributes="Italic" />

                            <Entry
                                Text="{Binding AmountText}"
                                Placeholder="Ex: 25.50"
                                Keyboard="Numeric"
                                TextColor="{StaticResource GoldText}"
                                PlaceholderColor="{StaticResource GoldText}"
                                BackgroundColor="#1A1A1A"
                                FontSize="16"
                                HeightRequest="50" />
                        </VerticalStackLayout>

                        <Button
                            Text="Calculer les points"
                            Command="{Binding ValidateAmountCommand}"
                            BackgroundColor="{StaticResource GoldText}"
                            TextColor="#0B0B0B"
                            FontAttributes="Bold"
                            CornerRadius="10"
                            HeightRequest="50"
                            Margin="0,8,0,0" />
                    </VerticalStackLayout>
                </Border>

                <!-- Confirmation Ajout Points -->
                <Border
                    IsVisible="{Binding ShowConfirmation}"
                    Stroke="#000000"
                    StrokeThickness="1.5"
                    BackgroundColor="{StaticResource CardBackground}"
                    Padding="20"
                    StrokeShape="RoundRectangle 14">
                    <VerticalStackLayout Spacing="16">
                        <Label
                            Text="Confirmation"
                            FontSize="18"
                            FontAttributes="Bold"
                            TextColor="{StaticResource GoldText}" />

                        <BoxView
                            HeightRequest="1"
                            Color="{StaticResource GoldText}"
                            Opacity="0.3" />

                        <Label
                            Text="{Binding ConfirmationMessage}"
                            FontSize="14"
                            TextColor="{StaticResource GoldText}"
                            LineBreakMode="WordWrap" />

                        <HorizontalStackLayout Spacing="12" Margin="0,8,0,0">
                            <Button
                                Text="Confirmer"
                                Command="{Binding AddPointsCommand}"
                                BackgroundColor="{StaticResource GoldText}"
                                TextColor="#0B0B0B"
                                FontAttributes="Bold"
                                CornerRadius="10"
                                HeightRequest="50"
                                HorizontalOptions="FillAndExpand" />

                            <Button
                                Text="Annuler"
                                Command="{Binding CancelAddCommand}"
                                BackgroundColor="Transparent"
                                BorderColor="{StaticResource GoldText}"
                                BorderWidth="1.5"
                                TextColor="{StaticResource GoldText}"
                                CornerRadius="10"
                                HeightRequest="50"
                                WidthRequest="100" />
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Overlay Rush Config -->
        <Grid Grid.Row="0" Grid.RowSpan="2"
              IsVisible="{Binding ShowRushConfig}"
              BackgroundColor="#CC000000">
            <Border
                WidthRequest="320"
                HeightRequest="280"
                Stroke="{StaticResource RushColor}"
                StrokeThickness="2"
                BackgroundColor="{StaticResource CardBackground}"
                StrokeShape="RoundRectangle 20"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Padding="24">
                <VerticalStackLayout Spacing="16">
                    <!-- Header avec icône -->
                    <HorizontalStackLayout Spacing="12" HorizontalOptions="Center">
                        <Label
                            Text="⚡"
                            FontSize="28"
                            TextColor="{StaticResource RushColor}"
                            VerticalOptions="Center" />
                        <Label
                            Text="Mode Rush"
                            FontSize="22"
                            FontAttributes="Bold"
                            TextColor="{StaticResource RushColor}"
                            VerticalOptions="Center" />
                    </HorizontalStackLayout>

                    <Label
                        Text="Montant à créditer pour chaque scan :"
                        FontSize="14"
                        TextColor="{StaticResource GoldText}"
                        HorizontalTextAlignment="Center" />

                    <Entry
                        Text="{Binding RushAmountText}"
                        Placeholder="Ex: 15.00"
                        Keyboard="Numeric"
                        TextColor="{StaticResource GoldText}"
                        PlaceholderColor="#666"
                        BackgroundColor="#1A1A1A"
                        FontSize="18"
                        HeightRequest="50"
                        HorizontalTextAlignment="Center" />

                    <HorizontalStackLayout Spacing="12" HorizontalOptions="Center">
                        <Button
                            Text="Activer"
                            Command="{Binding ValidateRushAmountCommand}"
                            BackgroundColor="{StaticResource RushColor}"
                            TextColor="White"
                            FontAttributes="Bold"
                            CornerRadius="10"
                            HeightRequest="50"
                            WidthRequest="120" />

                        <Button
                            Text="Annuler"
                            Command="{Binding CloseRushConfigCommand}"
                            BackgroundColor="Transparent"
                            BorderColor="{StaticResource GoldText}"
                            BorderWidth="1.5"
                            TextColor="{StaticResource GoldText}"
                            CornerRadius="10"
                            HeightRequest="50"
                            WidthRequest="100" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>
        </Grid>

        <!-- Rush Status Indicator (Success/Error overlay) -->
        <Grid Grid.Row="0" Grid.RowSpan="2"
              IsVisible="{Binding ShowRushStatus}"
              InputTransparent="True">
            <Border
                WidthRequest="200"
                HeightRequest="200"
                StrokeShape="RoundRectangle 100"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Padding="20">
                <Border.Triggers>
                    <DataTrigger TargetType="Border" Binding="{Binding RushStatusSuccess}" Value="True">
                        <Setter Property="BackgroundColor" Value="{StaticResource SuccessColor}" />
                        <Setter Property="Stroke" Value="{StaticResource SuccessColor}" />
                    </DataTrigger>
                    <DataTrigger TargetType="Border" Binding="{Binding RushStatusSuccess}" Value="False">
                        <Setter Property="BackgroundColor" Value="{StaticResource ErrorColor}" />
                        <Setter Property="Stroke" Value="{StaticResource ErrorColor}" />
                    </DataTrigger>
                </Border.Triggers>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="8">
                    <Label
                        FontSize="60"
                        HorizontalOptions="Center"
                        TextColor="White">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Binding="{Binding RushStatusSuccess}" Value="True">
                                <Setter Property="Text" Value="✓" />
                            </DataTrigger>
                            <DataTrigger TargetType="Label" Binding="{Binding RushStatusSuccess}" Value="False">
                                <Setter Property="Text" Value="✗" />
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>
                    <Label
                        Text="{Binding RushStatusMessage}"
                        FontSize="12"
                        TextColor="White"
                        HorizontalTextAlignment="Center"
                        LineBreakMode="WordWrap"
                        MaxLines="3" />
                </VerticalStackLayout>
            </Border>
        </Grid>

    </Grid>
</ContentPage>
