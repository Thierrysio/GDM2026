<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GDM2026.ActualitePage"
             BackgroundColor="#0B0B0B">
    <ContentPage.Resources>
        <Color x:Key="GoldText">#D4AF37</Color>
        <Color x:Key="CardBackground">#151515</Color>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="18">
            <Border
                Background="{StaticResource CardBackground}"
                Stroke="#000000"
                StrokeThickness="1.5"
                Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="18" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="14">
                    <Label
                        Text="Actualités"
                        FontSize="28"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}" />

                    <Label
                        Text="Publiez rapidement une actualité avec une image pour l'afficher sur Dantec Market."
                        FontSize="16"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}"
                        Opacity="0.85" />

                    <VerticalStackLayout Spacing="10">
                        <Label
                            Text="Actualités publiées (admin)"
                            FontAttributes="Bold"
                            TextColor="{StaticResource GoldText}"
                            FontSize="18" />

                        <Label
                            Text="{Binding ExistingActualitesMessage}"
                            TextColor="{StaticResource GoldText}"
                            FontSize="13"
                            Opacity="0.85" />

                        <ActivityIndicator
                            IsRunning="{Binding IsActualitesLoading}"
                            IsVisible="{Binding IsActualitesLoading}"
                            Color="{StaticResource GoldText}" />

                        <CollectionView
                            ItemsSource="{Binding ExistingActualites}"
                            HeightRequest="320">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout ItemSpacing="8" Orientation="Vertical" />
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#111" Stroke="#2D2D2D" StrokeThickness="1" Padding="8" Margin="2">
                                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12" RowDefinitions="Auto,Auto">
                                            <Border Grid.RowSpan="2" Background="#1A1A1A" Stroke="#2D2D2D" StrokeThickness="1" CornerRadius="10">
                                                <Image
                                                    Source="{Binding FullImageUrl}"
                                                    Aspect="AspectFill"
                                                    HeightRequest="90"
                                                    WidthRequest="120" />
                                            </Border>

                                            <Label
                                                Grid.Column="1"
                                                Text="{Binding DisplayTitle}"
                                                FontAttributes="Bold"
                                                FontSize="14"
                                                TextColor="{StaticResource GoldText}" />

                                            <Label
                                                Grid.Column="1"
                                                Grid.Row="1"
                                                Text="{Binding DisplayDescription}"
                                                FontSize="12"
                                                LineBreakMode="WordWrap"
                                                TextColor="{StaticResource GoldText}"
                                                Opacity="0.85" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <Button
                            Text="Actualiser les actualités"
                            Command="{Binding RefreshActualitesCommand}"
                            BackgroundColor="#303030"
                            TextColor="{StaticResource GoldText}" />
                    </VerticalStackLayout>

                    <Button
                        Text="+ Nouvelle actualité"
                        BackgroundColor="#303030"
                        TextColor="{StaticResource GoldText}"
                        Command="{Binding ToggleFormCommand}" />

                    <Frame
                        IsVisible="{Binding IsFormVisible}"
                        BackgroundColor="#1E1E1E"
                        Padding="14"
                        HasShadow="False"
                        CornerRadius="14"
                        BorderColor="#2D2D2D">
                        <VerticalStackLayout Spacing="12">
                            <Label
                                Text="Titre"
                                FontAttributes="Bold"
                                TextColor="{StaticResource GoldText}" />
                            <Entry
                                Placeholder="Titre de l'actualité"
                                Text="{Binding ActualiteTitle}"
                                TextColor="{StaticResource GoldText}"
                                PlaceholderColor="#777"
                                BackgroundColor="#121212" />

                            <Label
                                Text="Contenu"
                                FontAttributes="Bold"
                                TextColor="{StaticResource GoldText}" />
                            <Editor
                                HeightRequest="140"
                                Text="{Binding ActualiteContent}"
                                Placeholder="Décrivez brièvement l'actualité"
                                TextColor="{StaticResource GoldText}"
                                PlaceholderColor="#777"
                                BackgroundColor="#121212" />

                            <Label
                                Text="Bibliothèque d'images (admin)"
                                FontAttributes="Bold"
                                TextColor="{StaticResource GoldText}" />

                            <Label
                                Text="{Binding ImageLibraryMessage}"
                                TextColor="{StaticResource GoldText}"
                                FontSize="13"
                                Opacity="0.85" />

                            <ActivityIndicator
                                IsRunning="{Binding IsImageLibraryLoading}"
                                IsVisible="{Binding IsImageLibraryLoading}"
                                Color="{StaticResource GoldText}" />

                            <CollectionView
                                ItemsSource="{Binding ImageLibrary}"
                                SelectionMode="Single"
                                SelectedItem="{Binding SelectedLibraryImage}"
                                HeightRequest="260">
                                <CollectionView.ItemsLayout>
                                    <GridItemsLayout Orientation="Vertical" Span="3" />
                                </CollectionView.ItemsLayout>
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#111" Stroke="#2D2D2D" StrokeThickness="1" Padding="6" Margin="4">
                                            <VerticalStackLayout Spacing="6">
                                                <Image
                                                    Source="{Binding FullUrl}"
                                                    Aspect="AspectFill"
                                                    HeightRequest="80"
                                                    WidthRequest="100" />
                                                <Label
                                                    Text="{Binding DisplayName}"
                                                    TextColor="{StaticResource GoldText}"
                                                    FontSize="11"
                                                    LineBreakMode="TailTruncation"
                                                    HorizontalTextAlignment="Center" />
                                            </VerticalStackLayout>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>

                            <Label
                                Text="{Binding SelectedImageName}"
                                TextColor="{StaticResource GoldText}"
                                FontSize="13"
                                Opacity="0.85" />

                            <VerticalStackLayout Spacing="6">
                                <Label
                                    Text="Renommer la photo sélectionnée"
                                    FontAttributes="Bold"
                                    TextColor="{StaticResource GoldText}" />
                                <Entry
                                    Placeholder="Nom personnalisé pour la photo"
                                    Text="{Binding SelectedImageCustomName}"
                                    IsEnabled="{Binding HasSelectedImage}"
                                    TextColor="{StaticResource GoldText}"
                                    PlaceholderColor="#777"
                                    BackgroundColor="#121212" />
                            </VerticalStackLayout>

                            <Border Background="#111" Padding="10" Stroke="#2D2D2D" StrokeThickness="1" IsVisible="{Binding HasSelectedImage}">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12" />
                                </Border.StrokeShape>
                                <Image
                                    Source="{Binding SelectedImage}"
                                    Aspect="AspectFit"
                                    HeightRequest="220"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center" />
                            </Border>

                            <Button
                                Text="Ajouter une actualité"
                                Command="{Binding SubmitCommand}"
                                BackgroundColor="#C18F1A"
                                TextColor="{StaticResource GoldText}" />
                        </VerticalStackLayout>
                    </Frame>

                    <Label
                        Text="{Binding StatusMessage}"
                        TextColor="{Binding StatusColor}"
                        FontSize="14"
                        HorizontalTextAlignment="Center"
                        LineBreakMode="WordWrap" />
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
