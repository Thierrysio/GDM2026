<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GDM2026.PartnersPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="PartnersRoot"
    Title="Partenaires"
    BackgroundColor="Black">

    <ContentPage.Content>
        <VerticalStackLayout Padding="16" Spacing="12">

            <!-- Titre -->
            <Label
                Text="Nos partenaires"
                FontSize="24"
                FontAttributes="Bold"
                TextColor="White" />

            <!-- Message de statut (chargement / erreurs / nb de partenaires) -->
            <Label
                Text="{Binding StatusMessage}"
                FontSize="12"
                TextColor="LightGray" />

            <!-- Liste des partenaires avec Pull-to-Refresh -->
            <RefreshView
                Command="{Binding RefreshCommand}"
                IsRefreshing="{Binding IsRefreshing}">

                <CollectionView
                    ItemsSource="{Binding Partners}">
                    <CollectionView.EmptyView>
                        <Label
                            Text="Aucun partenaire à afficher."
                            HorizontalOptions="Center"
                            VerticalOptions="Center"
                            TextColor="White" />
                    </CollectionView.EmptyView>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame
                                Padding="10"
                                Margin="0,4"
                                HasShadow="False"
                                BorderColor="DarkGray"
                                BackgroundColor="#111111">

                                <Grid
                                    ColumnDefinitions="80,*"
                                    ColumnSpacing="10">

                                    <!-- Image / logo du partenaire depuis la page admin (avec cache) -->
                                    <Image
                                        Grid.Column="0"
                                        WidthRequest="64"
                                        HeightRequest="64"
                                        Aspect="AspectFit"
                                        VerticalOptions="Center"
                                        IsVisible="{Binding HasImage}">
                                        <Image.Source>
                                            <UriImageSource Uri="{Binding FullImageUrl}" CachingEnabled="True" CacheValidity="7.00:00:00" />
                                        </Image.Source>
                                    </Image>

                                    <!-- Fallback si aucune image -->
                                    <Label
                                        Grid.Column="0"
                                        HorizontalTextAlignment="Center"
                                        VerticalTextAlignment="Center"
                                        Text="Pas d'image"
                                        FontSize="10"
                                        TextColor="LightGray"
                                        IsVisible="{Binding HasImage, Converter={StaticResource InverseBoolConverter}}" />

                                    <!-- Texte : nom + URL cliquable -->
                                    <VerticalStackLayout
                                        Grid.Column="1"
                                        Spacing="4"
                                        VerticalOptions="Center">

                                        <!-- Nom du partenaire -->
                                        <Label
                                            Text="{Binding DisplayName}"
                                            FontAttributes="Bold"
                                            FontSize="16"
                                            TextColor="White" />

                                        <!-- URL du partenaire (affichée + cliquable) -->
                                        <Label
                                            Text="{Binding WebsiteDisplay}"
                                            FontSize="12"
                                            TextColor="#66CCFF">
                                            <Label.GestureRecognizers>
                                                <TapGestureRecognizer
                                                    Command="{Binding BindingContext.OpenWebsiteCommand, Source={x:Reference PartnersRoot}}"
                                                    CommandParameter="{Binding Website}" />
                                            </Label.GestureRecognizers>
                                        </Label>

                                    </VerticalStackLayout>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </RefreshView>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>
