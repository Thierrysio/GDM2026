<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GDM2026.PartnersPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="PartnersRoot"
    BackgroundColor="#0B0B0B">

    <ContentPage.Resources>
        <Color x:Key="GoldText">#D4AF37</Color>
        <Color x:Key="CardBackground">#151515</Color>
        <Color x:Key="FieldBg">#121212</Color>
        <Color x:Key="StrokeDark">#2D2D2D</Color>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="18">
            <Border
                Background="{StaticResource CardBackground}"
                Stroke="#000000"
                StrokeThickness="1.5"
                Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="18" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="16">
                    <Label
                        Text="Gestion des partenaires"
                        FontSize="28"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}" />

                    <Label
                        Text="Ajoutez ou mettez à jour les partenaires de Dantec Market avec un flux cohérent."
                        FontSize="16"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}"
                        Opacity="0.85" />

                    <VerticalStackLayout Spacing="10">
                        <Button
                            Text="+ Nouveau partenaire"
                            BackgroundColor="#303030"
                            TextColor="{StaticResource GoldText}"
                            Command="{Binding ShowCreatePanelCommand}" />
                        <Button
                            Text="Mettre à jour un partenaire"
                            BackgroundColor="#303030"
                            TextColor="{StaticResource GoldText}"
                            Command="{Binding ShowUpdatePanelCommand}" />
                        <Button
                            Text="Charger / Recharger les partenaires"
                            BackgroundColor="#303030"
                            TextColor="{StaticResource GoldText}"
                            Command="{Binding LoadPartnersCommand}" />
                    </VerticalStackLayout>

                    <Border
                        IsVisible="{Binding IsUpdateMode}"
                        Background="#1E1E1E"
                        Padding="14"
                        Stroke="#2D2D2D">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="14" />
                        </Border.StrokeShape>
                        <VerticalStackLayout Spacing="12">
                            <Label
                                Text="Sélectionnez un partenaire à modifier"
                                FontAttributes="Bold"
                                TextColor="{StaticResource GoldText}" />

                            <ActivityIndicator
                                IsRunning="{Binding IsLoading}"
                                IsVisible="{Binding IsLoading}"
                                Color="{StaticResource GoldText}" />

                            <CollectionView
                                ItemsSource="{Binding Partners}"
                                SelectionMode="Single"
                                SelectedItem="{Binding SelectedPartner}"
                                HeightRequest="240">
                                <CollectionView.ItemsLayout>
                                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="8" />
                                </CollectionView.ItemsLayout>
                                <CollectionView.EmptyView>
                                    <Label
                                        Text="Aucun partenaire chargé. Utilisez le bouton de chargement."
                                        TextColor="{StaticResource GoldText}"
                                        HorizontalTextAlignment="Center"
                                        Opacity="0.6" />
                                </CollectionView.EmptyView>
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#111" Stroke="#2D2D2D" StrokeThickness="1" Padding="10" Margin="0,4">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="12" />
                                            </Border.StrokeShape>
                                            <VerticalStackLayout Spacing="4">
                                                <Label
                                                    Text="{Binding Id, StringFormat='Id : {0}'}"
                                                    TextColor="{StaticResource GoldText}"
                                                    FontSize="13" />
                                                <Label
                                                    Text="{Binding DisplayName}"
                                                    TextColor="{StaticResource GoldText}"
                                                    FontAttributes="Bold"
                                                    LineBreakMode="TailTruncation" />
                                                <Label
                                                    Text="{Binding WebsiteDisplay}"
                                                    TextColor="#66CCFF"
                                                    FontSize="12"
                                                    LineBreakMode="TailTruncation" />
                                            </VerticalStackLayout>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>

                            <Label
                                Text="{Binding StatusMessage}"
                                TextColor="{StaticResource GoldText}"
                                FontSize="13"
                                Opacity="0.85"
                                HorizontalTextAlignment="Center" />

                            <Button
                                Text="Supprimer le partenaire sélectionné"
                                Command="{Binding DeleteCommand}"
                                BackgroundColor="#4A1010"
                                TextColor="{StaticResource GoldText}"
                                IsEnabled="{Binding HasPartnerSelection}" />
                        </VerticalStackLayout>
                    </Border>

                    <Border
                        IsVisible="{Binding IsFormSectionVisible}"
                        Background="#1E1E1E"
                        Padding="14"
                        Stroke="#2D2D2D">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="14" />
                        </Border.StrokeShape>
                        <VerticalStackLayout Spacing="12">
                            <Label
                                Text="{Binding FormHeader}"
                                FontAttributes="Bold"
                                TextColor="{StaticResource GoldText}"
                                FontSize="20" />

                            <Label
                                Text="{Binding FormHelperMessage}"
                                TextColor="{StaticResource GoldText}"
                                FontSize="13"
                                Opacity="0.8" />

                            <VerticalStackLayout Spacing="12" IsEnabled="{Binding IsFormInputEnabled}">
                                <Label
                                    Text="Nom"
                                    FontAttributes="Bold"
                                    TextColor="{StaticResource GoldText}" />
                                <Entry
                                    Placeholder="Nom du partenaire"
                                    Text="{Binding PartnerName}"
                                    TextColor="{StaticResource GoldText}"
                                    PlaceholderColor="#777"
                                    BackgroundColor="{StaticResource FieldBg}" />

                                <Label
                                    Text="Site web"
                                    FontAttributes="Bold"
                                    TextColor="{StaticResource GoldText}" />
                                <Entry
                                    Placeholder="Site web (optionnel)"
                                    Text="{Binding PartnerWebsite}"
                                    TextColor="{StaticResource GoldText}"
                                    PlaceholderColor="#777"
                                    BackgroundColor="{StaticResource FieldBg}" />

                                <Label
                                    Text="Bibliothèque de logos"
                                    FontAttributes="Bold"
                                    TextColor="{StaticResource GoldText}" />

                                <SearchBar
                                    Placeholder="Rechercher un logo"
                                    Text="{Binding ImageSearchTerm}"
                                    BackgroundColor="{StaticResource FieldBg}"
                                    TextColor="{StaticResource GoldText}"
                                    PlaceholderColor="#777"
                                    CancelButtonColor="{StaticResource GoldText}" />

                                <Label
                                    Text="{Binding ImageLibraryMessage}"
                                    TextColor="{StaticResource GoldText}"
                                    FontSize="13"
                                    Opacity="0.85" />

                                <ActivityIndicator
                                    IsRunning="{Binding IsImageLibraryLoading}"
                                    IsVisible="{Binding IsImageLibraryLoading}"
                                    Color="{StaticResource GoldText}" />

                                <CollectionView
                                    ItemsSource="{Binding FilteredImageLibrary}"
                                    SelectionMode="Single"
                                    SelectedItem="{Binding SelectedLibraryImage}"
                                    HeightRequest="240">
                                    <CollectionView.ItemsLayout>
                                        <GridItemsLayout Orientation="Vertical" Span="3" />
                                    </CollectionView.ItemsLayout>
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#111" Stroke="{StaticResource StrokeDark}" StrokeThickness="1" Padding="6" Margin="4">
                                                <VerticalStackLayout Spacing="6">
                                                    <Image
                                                        Source="{Binding FullUrl}"
                                                        Aspect="AspectFill"
                                                        HeightRequest="80"
                                                        WidthRequest="100" />
                                                    <Label
                                                        Text="{Binding DisplayName}"
                                                        TextColor="{StaticResource GoldText}"
                                                        FontSize="11"
                                                        LineBreakMode="TailTruncation"
                                                        HorizontalTextAlignment="Center" />
                                                </VerticalStackLayout>
                                            </Border>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>

                                <Label
                                    Text="{Binding SelectedImageName}"
                                    TextColor="{StaticResource GoldText}"
                                    FontSize="13"
                                    Opacity="0.85" />

                                <Button
                                    Text="Effacer le logo"
                                    Command="{Binding ClearLogoCommand}"
                                    BackgroundColor="#303030"
                                    TextColor="{StaticResource GoldText}" />

                                <Border Background="#111" Padding="10" Stroke="{StaticResource StrokeDark}" StrokeThickness="1" IsVisible="{Binding HasSelectedImage}">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="12" />
                                    </Border.StrokeShape>
                                    <Image
                                        Source="{Binding SelectedImagePreview}"
                                        Aspect="AspectFit"
                                        HeightRequest="220"
                                        HorizontalOptions="Center"
                                        VerticalOptions="Center" />
                                </Border>
                            </VerticalStackLayout>

                            <Button
                                Text="{Binding FormActionButtonText}"
                                Command="{Binding FormActionCommand}"
                                BackgroundColor="#C18F1A"
                                TextColor="{StaticResource GoldText}"
                                IsEnabled="{Binding IsFormInputEnabled}" />
                        </VerticalStackLayout>
                    </Border>

                    <Label
                        Text="{Binding StatusMessage}"
                        TextColor="{StaticResource GoldText}"
                        FontSize="14"
                        HorizontalTextAlignment="Center"
                        LineBreakMode="WordWrap" />
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
