<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GDM2026.PartnersPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="PartnersRoot"
    Title="Partenaires">

    <ContentPage.Content>
        <VerticalStackLayout Padding="16" Spacing="12">

            <!-- Titre -->
            <Label
                Text="Nos partenaires"
                FontSize="24"
                FontAttributes="Bold" />

            <!-- Message de statut (chargement / erreurs / nb de partenaires) -->
            <Label
                Text="{Binding StatusMessage}"
                FontSize="12"
                TextColor="Gray" />

            <!-- Liste des partenaires avec Pull-to-Refresh -->
            <RefreshView
                Command="{Binding RefreshCommand}"
                IsRefreshing="{Binding IsRefreshing}">

                <CollectionView
                    ItemsSource="{Binding Partners}">
                    <CollectionView.EmptyView>
                        <Label
                            Text="Aucun partenaire à afficher."
                            HorizontalOptions="Center"
                            VerticalOptions="Center" />
                    </CollectionView.EmptyView>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame
                                Padding="10"
                                Margin="0,4"
                                HasShadow="False"
                                BorderColor="LightGray">

                                <Grid
                                    ColumnDefinitions="80,*"
                                    ColumnSpacing="10">

                                    <!-- Image / logo du partenaire -->
                                    <Image
                                        Grid.Column="0"
                                        WidthRequest="64"
                                        HeightRequest="64"
                                        Aspect="AspectFit"
                                        VerticalOptions="Center"
                                        Source="{Binding LogoUrl}" />

                                    <!-- Texte : nom + URL cliquable -->
                                    <VerticalStackLayout
                                        Grid.Column="1"
                                        Spacing="4"
                                        VerticalOptions="Center">

                                        <!-- Nom du partenaire -->
                                        <Label
                                            Text="{Binding Name}"
                                            FontAttributes="Bold"
                                            FontSize="16" />

                                        <!-- URL du partenaire (affichée + cliquable) -->
                                        <Label
                                            Text="{Binding WebsiteUrl}"
                                            FontSize="12"
                                            TextColor="Blue">
                                            <Label.GestureRecognizers>
                                                <TapGestureRecognizer
                                                    Command="{Binding BindingContext.OpenWebsiteCommand, Source={x:Reference PartnersRoot}}"
                                                    CommandParameter="{Binding WebsiteUrl}" />
                                            </Label.GestureRecognizers>
                                        </Label>

                                    </VerticalStackLayout>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </RefreshView>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>
