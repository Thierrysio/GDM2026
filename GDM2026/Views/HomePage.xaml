<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GDM2026.HomePage"
             x:Name="HomePageRoot"
             BackgroundColor="#0B0B0B">
    <ContentPage.Resources>
        <Color x:Key="GoldText">#D4AF37</Color>
        <Color x:Key="CardBackground">#151515</Color>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout
            Padding="24"
            Spacing="18">
            <VerticalStackLayout
                HorizontalOptions="Center"
                Spacing="8"
                Margin="0,12,0,6">
                <Border
                    HeightRequest="160"
                    WidthRequest="160"
                    Padding="12"
                    Background="White"
                    Stroke="#E0E0E0"
                    StrokeThickness="1">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="80" />
                    </Border.StrokeShape>
                    <Image
                        Source="logo.jpg"
                        Aspect="AspectFit"
                        AutomationId="HomeHeaderLogo" />
                </Border>

                <Label
                    Text="Dantec Market"
                    FontSize="32"
                    FontAttributes="Bold"
                    HorizontalTextAlignment="Center"
                    TextColor="{StaticResource GoldText}" />
            </VerticalStackLayout>

            <Border
                Background="{StaticResource CardBackground}"
                Stroke="#000000"
                StrokeThickness="1.5"
                Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="18" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="14">
                    <Label
                        Text="Club Dantec Market"
                        FontSize="28"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}" />

                    <Label
                        Text="Rejoignez le club et alimentez votre porte-monnaie fidélité grâce à vos couronnes."
                        FontSize="16"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}"
                        Opacity="0.9" />

                    <Border
                        Background="#1A1A1A"
                        Stroke="#2A2A2A"
                        StrokeThickness="1"
                        Padding="14">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="16" />
                        </Border.StrokeShape>
                        <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="10" VerticalOptions="Center">
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label
                                    Text="Porte-monnaie fidélité"
                                    FontAttributes="Bold"
                                    TextColor="{StaticResource GoldText}" />
                                <Label
                                    Text="{Binding LoyaltyBalanceText}"
                                    FontSize="15"
                                    TextColor="{StaticResource GoldText}" />
                            </VerticalStackLayout>

                            <Button
                                Grid.Column="1"
                                Text="Panier"
                                BackgroundColor="#C18F1A"
                                TextColor="{StaticResource GoldText}" />

                            <Button
                                Grid.Column="2"
                                Text="☰"
                                FontAttributes="Bold"
                                BackgroundColor="#252525"
                                TextColor="{StaticResource GoldText}"
                                Command="{Binding ToggleProfileMenuCommand}" />
                        </Grid>
                    </Border>

                    <Border
                        IsVisible="{Binding IsProfileMenuVisible}"
                        Background="#111"
                        Stroke="#2D2D2D"
                        Padding="14"
                        StrokeThickness="1">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="14" />
                        </Border.StrokeShape>
                        <VerticalStackLayout Spacing="10">
                            <Button
                                Text="Porte monnaie fidélité"
                                BackgroundColor="#C18F1A"
                                TextColor="{StaticResource GoldText}" />
                            <Button
                                Text="Mon profil"
                                BackgroundColor="#252525"
                                TextColor="{StaticResource GoldText}" />
                            <Button
                                Text="À propos"
                                BackgroundColor="#252525"
                                TextColor="{StaticResource GoldText}" />
                            <Button
                                Text="Se déconnecter"
                                BackgroundColor="#3A1B1B"
                                TextColor="{StaticResource GoldText}" />
                        </VerticalStackLayout>
                    </Border>
                </VerticalStackLayout>
            </Border>

            <Border
                Background="{StaticResource CardBackground}"
                Stroke="#000000"
                StrokeThickness="1.5"
                Padding="24">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="18" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="12">
                    <Label
                        Text="Accueil"
                        FontSize="32"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}" />

                    <Label
                        FontSize="18"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}"
                        Text="{Binding WelcomeText}" />

                    <Label
                        Text="Vous êtes connecté à Dantec Market."
                        FontSize="16"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}" />
                </VerticalStackLayout>
            </Border>

            <Border
                Background="{StaticResource CardBackground}"
                Stroke="#000000"
                StrokeThickness="1.5"
                Padding="24">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="18" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="12">
                    <Label
                        Text="Commandes par état"
                        FontSize="22"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource GoldText}" />

                    <CollectionView
                        ItemsSource="{Binding OrderStatuses}"
                        SelectionMode="None">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Vertical" Span="2" />
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border
                                    Margin="6"
                                    Padding="12"
                                    Background="#1F1F1F"
                                    Stroke="#000000"
                                    StrokeThickness="1">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="14" />
                                    </Border.StrokeShape>
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding BindingContext.OrderStatusSelectedCommand, Source={x:Reference HomePageRoot}}"
                                            CommandParameter="{Binding .}" />
                                    </Border.GestureRecognizers>
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal">
                                                <VisualState.Setters>
                                                    <Setter Property="Background" Value="#1F1F1F" />
                                                    <Setter Property="Scale" Value="1" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Pressed">
                                                <VisualState.Setters>
                                                    <Setter Property="Background" Value="#2A2A2A" />
                                                    <Setter Property="Scale" Value="0.98" />
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                    <VerticalStackLayout Spacing="4">
                                        <Label
                                            Text="{Binding Status}"
                                            FontSize="16"
                                            FontAttributes="Bold"
                                            TextColor="{StaticResource GoldText}" />
                                        <Label
                                            Text="{Binding DisplayCount}"
                                            FontSize="14"
                                            TextColor="{StaticResource GoldText}"
                                            Opacity="0.85" />
                                    </VerticalStackLayout>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

            <Label
                Text="Catégories administrateur"
                FontSize="22"
                FontAttributes="Bold"
                TextColor="{StaticResource GoldText}" />

            <CollectionView
                ItemsSource="{Binding Categories}"
                SelectionMode="None"
                Margin="0,6,0,0">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="2" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Margin="6"
                            Padding="16"
                            Background="{StaticResource CardBackground}"
                            Stroke="#000000"
                            StrokeThickness="1">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="16" />
                            </Border.StrokeShape>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding BindingContext.CategorySelectedCommand, Source={x:Reference HomePageRoot}}"
                                    CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal">
                                        <VisualState.Setters>
                                            <Setter Property="Background" Value="{StaticResource CardBackground}" />
                                            <Setter Property="Scale" Value="1" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Property="Background" Value="#1E1E1E" />
                                            <Setter Property="Scale" Value="0.98" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <VerticalStackLayout Spacing="6">
                                <Label
                                    Text="{Binding Title}"
                                    FontSize="18"
                                    FontAttributes="Bold"
                                    TextColor="{StaticResource GoldText}" />
                                <Label
                                    Text="{Binding Description}"
                                    FontSize="13"
                                    TextColor="{StaticResource GoldText}"
                                    Opacity="0.75" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
